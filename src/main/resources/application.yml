spring:
  datasource:
    url: jdbc:postgresql://localhost:2001/short_course
    username: sochoeun
    password: pass
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: true
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect
#keycloadk
  security:
    oauth2:
      resourceserver:
        jwt:
          jwk-set-uri: http://localhost:8080/realms/short_course/protocol/openid-connect/certs
server:
  port: 8000


