spring:
  main:
    banner-mode: off
  datasource:
    url: jdbc:postgresql://localhost:5432/short_course
    username: postgres
    password: 089882413
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: true
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect
server:
  port: 8000
logging:
  level:
    root: warn


